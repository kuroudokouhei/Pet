type Poster @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String
  date: String
  location: String
  animal: String
  type: String
  age: String
  color: String
  character: String
  other: String
}

type CheckPoster @model @auth(rules: [{allow: public}]) {
  id: ID!
  main: String
}
 